# 边界迁移清单

本文是《架构边界设计》的执行清单，用于冻结现有协议的目标所有者。迁移完成前，类型仍可暂时位于原程序集，但不得继续向错误的所有者增加定义。

## 所有权判定规则

- 内核拥有可复用机制和不变量，不拥有实验学科词汇。
- 模块拥有自己产生、解释和校验的协议标识。
- 课程只引用模块协议并提供实例数据，不声明新的运行时协议。
- 表现层只消费科学事件，不回写权威科学状态。
- Unity 适配层只负责场景输入、空间事实和表现执行。

## 现有定义迁移

| 现有定义 | 当前程序集 | 目标所有者 | 处理方式 | 阶段 |
| --- | --- | --- | --- | --- |
| `EntityId`、`SimulationTick` | Kernel | 最小内核 | 保留 | 三 |
| `Quantity` 的数值表达 | Kernel | 最小内核 | 收敛为通用量值 | 三 |
| `Temperature` | Kernel | 测量模块 | 迁出具体温标语义 | 三 |
| `ExperimentEntity`、`ExperimentWorld` | Domain | 最小内核 | 保留实体和世界机制，移除物质语义 | 三 |
| `Matter/*` | Domain | 化学模块 | 整体迁移 | 三 |
| `ICapability` | Domain | 最小内核 | 由通用组件契约替代 | 三 |
| `CoreCapabilities` | Domain | 交互模块、化学模块 | 按协议所有权拆分 | 三 |
| `RelationGraph`、`EntityRelation` | Domain | 最小内核 | 保留关系存储、索引和约束执行机制 | 三 |
| `RelationKind` | Domain | 各声明模块 | 替换为类型化关系标识及关系模式注册 | 三、六 |
| `ProcessScheduler`、`IProcessHandler` | Domain | 最小内核 | 保留确定性调度机制 | 三 |
| `SemanticActionRequest`、`SemanticActionOutcome` | Application | 最小内核应用契约 | 保留通用动作请求和裁决 | 三 |
| `CoreSemanticActionIds` | Application | 交互模块 | 迁移抓取、释放、放置、连接等动作 | 三 |
| `StructuredFactField` 值对象 | Application | 最小内核应用契约 | 保留标识值对象 | 三 |
| `StructuredFactField` 静态字段 | Application | 交互、空间、教学等模块 | 按字段解释者拆分 | 三、六 |
| `StructuredRuleDefinition`、`StructuredRuleEvaluator` | Application | 最小内核应用契约 | 保留类型化规则机制 | 三 |
| `ConfiguredStateOperationRegistry` | Application | 最小内核应用契约 | 保留注册、冻结和原子执行机制 | 二 |
| 内置关系、标量、过程、事件状态操作 | Application | 相应关系、状态、过程模块 | 从注册表构造函数迁至模块清单 | 二、三 |
| `CoreCourseRegistrations` | Application | 模块组合入口 | 拆分为显式模块注册 | 二、三 |
| `CourseConfigurationKeys` | Application | 创作编译契约及模块模式 | 不进入最小运行时内核 | 四、六 |
| 目标、评分、提示和后果定义 | Application | 教学模块 | 迁移教学语义，保留通用事件消费接口 | 三 |
| `Presentation*` | Presentation | 表现语义模块 | 保持只读消费科学结果 | 三 |
| `Chemistry*` | Chemistry Sample | 化学模块 | 保留学科协议，禁止氧气课程实体标识 | 三 |
| `UnityAdapters*` | UnityAdapter Sample | Unity 适配模块 | 保持依赖方向朝向内核和表现契约 | 二、三 |
| OxygenCourse 配置及总预制体 | OxygenCourse Sample | 氧气课程 | 只保留实例、覆盖和验收数据 | 五 |

## 第一阶段冻结规则

从本清单建立起执行以下限制：

1. 不向 `RelationKind`、`CoreCapabilityIds`、`CoreSemanticActionIds` 或 `StructuredFactField` 静态成员增加新协议。
2. 不在 `Runtime` 中增加化学、氧气课程或 Unity 专用引用。
3. 不让 Chemistry 引用 OxygenCourse，也不让 UnityAdapter 的通用运行时引用 Chemistry。
4. 不新增横向动作专用课程列；新配置需求先归入模块参数模式。
5. 迁移产生破坏性变化时一次性修改调用方，不添加 `.vN` 或旧协议分支。

## 提交切分

- 阶段一：迁移清单和程序集依赖守卫。
- 阶段二：模块清单、注册上下文、冻结及冲突校验。
- 阶段三：交互、测量、化学和教学协议迁移。
- 阶段四：组件与选择器创作模型及课程编译器切换。
- 阶段五：氧气课程迁移与行为验收。
- 阶段六：删除旧定义、旧列和临时适配。
